# Development and test dependencies for the agent.
# Install with:
#   pip install -r requirements.txt -r requirements-dev.txt
#
# Not bundled into the AgentCore CodeZip deployment artifact.

-r requirements.txt

autopep8>=2.0.0,<3.0.0
pylint>=3.0.0,<4.0.0
pytest>=8.0.0,<9.0.0
